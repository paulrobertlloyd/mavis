{% extends "_layouts/form.njk" %}

{% set title = __("vaccination.decline.title", { campaign: campaign }) %}

{% block form %}
  {{ radios({
    fieldset: {
      legend: {
        html: heading({
          classes: "nhsuk-fieldset__legend--l",
          caption: patient.record.fullName,
          title: title
        })
      }
    },
    items: declineItems,
    decorate: "vaccination.outcome"
  }) }}
{% endblock %}
