{% extends "_layouts/default.njk" %}

{% set title = user.fullName %}
{% set paths = { back: "/users" } %}

{% block content %}
  <div class="nhsuk-u-width-three-quarters">
    {{ super() }}

    {{ heading({
      title: title
    }) }}

    {{ card({
      heading: "Details",
      headingClasses: "nhsuk-heading-m",
      descriptionHtml: summaryList({
        rows: summaryRows(user, {
          fullName: {
            href: user.uri + "/edit"
          },
          email: {
            href: user.uri + "/edit"
          },
          registrar: {
            value: Registrar[user.registrar],
            href: user.uri + "/edit"
          },
          registration: {
            href: user.uri + "/edit"
          }
        })
      })
    }) }}

    <p>
      <a href="{{ user.uri }}/delete">Delete user</a>
    </p>
  </div>
{% endblock %}
