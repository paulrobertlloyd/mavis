{% extends "_layouts/default.njk" %}

{% set public = true %}
{% set ra = "Acme Community Hospital NHS Foundation Trust (ACM)" %}
{% set type = data.type or "user" %}
{% set title = __("account.permissions." + type + ".title") %}

{% block content %}
  <div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-two-thirds">
      {{ heading({
        title: title
      }) }}

      {{ __("account.permissions.org.description", { ra: ra }) | nhsukMarkdown if type == "org" }}

      {{ link("#", __("account.cis2.change-role")) | nhsukMarkdown }}

      {% if type == "user" %}
        <h2 class="nhsuk-heading-m">Contact your registration authority</h2>

        {% filter nhsukMarkdown %}
If you think you should have permission to use this service, contact your registration authority, {{ ra }}. Ask for one of the following roles to be added to your Care Identity:

- Nurse Access Role (R8001)
- Medical Secretary Access Role (R8006)
- Admin/Clinical Support Access Role (R8008)
- Receptionist Access Role (R8009)
        {% endfilter %}
      {% endif %}
    </div>
  </div>
{% endblock %}
