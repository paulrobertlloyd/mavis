{% extends "_layouts/default.njk" %}

{% set title = __("consent.unavailable.title") %}

{% block content %}
  <div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-two-thirds">
      {{ heading({
        title: title
      }) }}

      {{ __("consent.unavailable.description") | nhsukMarkdown }}
    </div>
  </div>
{% endblock %}
